'use strict';
// postgredb schema
// create table shop(id serial primary key, companyName text, address text, about text, image text, phones text[], keywords text[], categories text[], facebook text, ratingTotal float, photos text[], menus text[], branches text[], reviews text[], mapUrl text)

module.exports = function(Shop) {

};
