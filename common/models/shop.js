'use strict';
// postgredb schema
// create table shop(id serial primary key, companyName varchar(30), address text, about text, image text, keywords text[], categories text[], facebook text, ratingTotal float, photos text[], menus text[], branches text[], reviews text[], mapUrl text)

module.exports = function(Shop) {

};
