'use strict';
// db schema
// create table shop(id serial primary key, companyName varchar(30), address text, about text, image text, keywords varchar(30)[], categories varchar(30)[], facebook text, ratingTotal float, photos text[], menus text[], branches text[], reviews text[])

module.exports = function(Shop) {

};
